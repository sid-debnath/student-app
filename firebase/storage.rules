rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /institutions/{institutionId}/{allPaths=**} {
      allow read: if request.auth != null && institutionId == 'default';
      allow write: if request.auth != null && institutionId == 'default';
    }
  }
}
